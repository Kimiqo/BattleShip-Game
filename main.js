import Player from "./game/players.js";

let name = prompt("Enter a name: ");
const p1 = new Player(name);
console.log(p1.playerboard.board);

let comp = "Computer";
const p1 = new Player(name);
console.log(p1.playerboard.board);